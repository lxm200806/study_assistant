---
description: 改完代码后主动启动后端与前端，方便用户测试
alwaysApply: true
---

# 改完后主动启动服务端 + 客户端

完成修改后，**不要只告诉用户自己启动**，应自行执行 `./start-dev.sh`（或分别启动 `./start-server.sh` 与 `./start-client.sh`），并验证两端均可访问。

## 何时启动

- 修改 `server/`、`src/`、词库、数据库、配置
- 重建词库或同步数据库后

## 命令

```bash
# 推荐：一键启动后端 3004 + 前端 5173
./start-dev.sh

# 或分别启动
./start-server.sh   # 后台阻塞运行
./start-client.sh   # 另开终端
```

## 验证

```bash
curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:3004/api/books  # 期望 200
curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:5173/            # 期望 200
```

## 访问地址

- 前端测试：http://localhost:5173
- API 文档：http://localhost:3004/api-docs
